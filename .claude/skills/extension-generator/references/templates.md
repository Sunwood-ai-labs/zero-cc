# 実用テンプレート集（v2.1.1+ 統合版）

よく使われるユースケース別のスキルテンプレート。

---

## 構成パターン別テンプレート

### パターン1: シンプル（SKILL.mdのみ）

```
skill-name/
└── SKILL.md
```

用途: 単純なプロンプト再利用、簡単な変換

---

### パターン2: 中程度（+references）

```
skill-name/
├── SKILL.md
└── references/
    ├── CHECKLIST.md
    └── EXAMPLES.md
```

用途: チェックリスト付きタスク、ガイドライン適用

---

### パターン3: 複雑（+scripts）

```
skill-name/
├── SKILL.md
├── references/
│   ├── SECURITY.md
│   └── PERFORMANCE.md
└── scripts/
    ├── lint.sh
    └── analyze.py
```

用途: 外部ツール連携、自動化タスク

---

## 完全なスキル例

### コードレビュースキル（フル構成）

**ディレクトリ構造:**
```
code-review/
├── SKILL.md
├── references/
│   ├── SECURITY.md
│   ├── PERFORMANCE.md
│   └── STYLE.md
└── scripts/
    └── run-linters.sh
```

**SKILL.md:**
```yaml
---
name: code-review
description: |
  包括的なコードレビューを実施。
  「レビューして」「PRチェックして」「コード見て」で発動。
allowed-tools: Read, Grep, Glob, Bash(./scripts/*)
---

# Code Review

## ワークフロー

1. **リンター実行**
   ```bash
   ./scripts/run-linters.sh
   ```

2. **セキュリティチェック**
   See [references/SECURITY.md](references/SECURITY.md)

3. **パフォーマンス分析**
   See [references/PERFORMANCE.md](references/PERFORMANCE.md)

4. **スタイル確認**
   See [references/STYLE.md](references/STYLE.md)

5. **総合レポート生成**

## 出力形式

- 🔴 **Critical**: 必ず修正
- 🟡 **Warning**: 修正推奨
- 🔵 **Suggestion**: 検討事項

## 追加コンテキスト

$ARGUMENTS
```

**references/SECURITY.md:**
```markdown
# セキュリティチェックリスト

## 入力検証
- [ ] SQLインジェクション対策
- [ ] XSS対策
- [ ] パストラバーサル対策
- [ ] コマンドインジェクション対策

## 認証・認可
- [ ] 認証バイパスの可能性
- [ ] 権限チェック漏れ
- [ ] セッション管理の問題

## 機密情報
- [ ] ハードコードされた認証情報
- [ ] ログへの機密情報出力
- [ ] エラーメッセージでの情報漏洩

## 依存関係
- [ ] 既知の脆弱性を持つライブラリ
- [ ] 古いバージョンの使用
```

**references/PERFORMANCE.md:**
```markdown
# パフォーマンスチェックリスト

## データベース
- [ ] N+1クエリ
- [ ] インデックス未使用
- [ ] 不要なJOIN
- [ ] 大量データの一括取得

## メモリ
- [ ] メモリリーク
- [ ] 大きなオブジェクトの保持
- [ ] 不要なキャッシュ

## ネットワーク
- [ ] 不要なAPI呼び出し
- [ ] キャッシュ未活用
- [ ] 同期的な外部呼び出し

## 計算
- [ ] 非効率なアルゴリズム
- [ ] 重複計算
- [ ] ループ内の重い処理
```

**references/STYLE.md:**
```markdown
# スタイルガイド

## 命名規約
- 変数: camelCase
- 定数: UPPER_SNAKE_CASE
- クラス: PascalCase
- ファイル: kebab-case

## コード構造
- 1関数 = 1責任
- 関数は50行以内
- ネストは3レベル以内
- マジックナンバー禁止

## コメント
- 「なぜ」を書く（「何」ではない）
- TODOには担当者と期限
- 複雑なロジックには説明
```

**scripts/run-linters.sh:**
```bash
#!/bin/bash
# コードレビュー用リンター実行スクリプト

set -e

echo "=== ESLint ==="
npm run lint 2>&1 || true

echo ""
echo "=== TypeScript ==="
npm run type-check 2>&1 || true

echo ""
echo "=== Prettier ==="
npm run format:check 2>&1 || true
```

---

### コミットメッセージ生成

```yaml
# .claude/skills/commit/SKILL.md
---
name: commit
description: |
  Conventional Commits形式のコミットメッセージを生成。
  「コミットして」「コミットメッセージ作って」で発動。
allowed-tools: Bash(git diff:*), Bash(git status:*), Bash(git add:*), Bash(git commit:*)
---

# Commit

## コンテキスト
- ステータス: !`git status --short`
- 差分: !`git diff --staged`
- ブランチ: !`git branch --show-current`

## ルール
- type: feat/fix/docs/style/refactor/test/chore
- scope: 変更対象のモジュール
- 本文は日本語OK

## 追加コンテキスト
$ARGUMENTS
```

### コードレビュー

```yaml
# .claude/skills/review/SKILL.md
---
name: review
description: |
  コードレビューを実施。セキュリティ、パフォーマンス、スタイルをチェック。
  「レビューして」「PRをチェックして」で発動。
allowed-tools: Read, Grep, Glob, Bash(git diff:*)
---

# Code Review

## 対象
$ARGUMENTS

## チェック項目
1. セキュリティ（SQLi, XSS, 認証）
2. パフォーマンス（N+1, メモリリーク）
3. コードスタイル（命名, 構造）
4. テストカバレッジ

## 出力形式
- 🔴 Critical: 必ず修正
- 🟡 Warning: 修正推奨
- 🔵 Suggestion: 検討事項
```

### テスト生成

```yaml
# .claude/skills/test/SKILL.md
---
name: test
description: |
  ユニットテストを生成。「テスト書いて」「テスト追加して」で発動。
allowed-tools: Read, Write, Bash(npm test:*), Bash(pytest:*)
---

# Test Generator

## 対象
$ARGUMENTS

## 要件
- 主要パス + エッジケース
- 外部依存はモック化
- 既存のテストスタイルに合わせる

## フレームワーク検出
- package.json に jest/vitest → Jest/Vitest
- pytest.ini / pyproject.toml → pytest
```

### リファクタリング

```yaml
# .claude/skills/refactor/SKILL.md
---
name: refactor
description: |
  コードのリファクタリングを提案・実施。
  「リファクタして」「きれいにして」で発動。
allowed-tools: Read, Write, Grep, Glob
---

# Refactor

## 対象
$ARGUMENTS

## 観点
1. 可読性
2. 保守性
3. パフォーマンス
4. テスタビリティ

## 出力
- Before/Afterコード
- 変更理由
- 影響範囲
```

## ドキュメント系

### README生成

```yaml
# .claude/skills/readme/SKILL.md
---
name: readme
description: |
  プロジェクトのREADMEを生成・更新。
  「README作って」「README更新して」で発動。
allowed-tools: Read, Write, Glob
---

# README Generator

## 参照
- package.json / pyproject.toml
- 既存のREADME（あれば）

## セクション
1. 概要
2. インストール
3. 使用方法
4. 設定
5. ライセンス

## 追加要件
$ARGUMENTS
```

### API仕様書

```yaml
# .claude/skills/api-doc/SKILL.md
---
name: api-doc
description: |
  コードからAPI仕様書を生成。OpenAPI形式。
  「API仕様書作って」で発動。
allowed-tools: Read, Write, Grep, Glob
---

# API Documentation

## 対象
$ARGUMENTS

## 形式
- OpenAPI 3.0
- 日本語説明
- リクエスト/レスポンス例を含める
```

## 分析系

### コード解説

```yaml
# .claude/skills/explain/SKILL.md
---
name: explain
description: |
  コードを初心者向けに解説。「説明して」「解説して」で発動。
allowed-tools: Read
---

# Code Explainer

## 対象
$ARGUMENTS

## 解説形式
1. 概要（1-2文）
2. 処理の流れ（箇条書き）
3. 重要なポイント
4. 関連する概念
```

### 依存関係チェック

```yaml
# .claude/skills/deps/SKILL.md
---
name: deps
description: |
  プロジェクトの依存関係を分析。
  「依存関係チェックして」「脆弱性確認して」で発動。
allowed-tools: Read, Bash(npm audit:*), Bash(pip-audit:*)
---

# Dependency Check

## チェック項目
1. 脆弱性
2. 古いパッケージ
3. ライセンス互換性
4. 未使用の依存関係

## 追加コンテキスト
$ARGUMENTS
```

## データ処理系

### データ分析

```yaml
# .claude/skills/analyze-data/SKILL.md
---
name: analyze-data
description: |
  CSV/JSONデータを分析。「データ分析して」「集計して」で発動。
allowed-tools: Read, Write, Bash(python:*)
---

# Data Analyzer

## 対象
$ARGUMENTS

## 分析内容
1. 基本統計量
2. データ品質チェック
3. 可視化（必要に応じて）
4. 洞察とサマリー
```

## Git操作系

### ブランチ整理

```yaml
# .claude/skills/git-cleanup/SKILL.md
---
name: git-cleanup
description: |
  マージ済みブランチの整理を提案。
  「ブランチ整理して」で発動。
allowed-tools: Bash(git branch:*), Bash(git log:*)
---

# Git Cleanup

## 現状
- ローカルブランチ: !`git branch`
- マージ済み: !`git branch --merged main`

## タスク
マージ済みで削除可能なブランチをリストアップし、
削除コマンドを生成（実行は確認後）。
```

### PR作成

```yaml
# .claude/skills/pr/SKILL.md
---
name: pr
description: |
  PRの説明文を生成。「PR作って」「PRの説明書いて」で発動。
allowed-tools: Bash(git diff:*), Bash(git log:*)
---

# PR Creator

## コンテキスト
- 変更差分: !`git diff main...HEAD --stat`
- コミット履歴: !`git log main..HEAD --oneline`

## 形式
- タイトル: Conventional Commits形式
- 本文: 変更概要、影響範囲、テスト方法

## 追加情報
$ARGUMENTS
```

## 複合スキル（複数ファイル）

### 完全なコードレビュースキル

```
.claude/skills/full-review/
├── SKILL.md
├── SECURITY.md
├── PERFORMANCE.md
└── scripts/
    └── run-linters.sh
```

**SKILL.md:**
```yaml
---
name: full-review
description: |
  包括的なコードレビュー。セキュリティ、パフォーマンス、
  コードスタイルを詳細にチェック。
allowed-tools: Read, Grep, Glob, Bash(./scripts/*)
---

# Full Code Review

## ワークフロー
1. リンター実行: `./scripts/run-linters.sh`
2. セキュリティチェック（See [SECURITY.md](SECURITY.md)）
3. パフォーマンス分析（See [PERFORMANCE.md](PERFORMANCE.md)）
4. 総合レポート生成

## 対象
$ARGUMENTS
```

**SECURITY.md:**
```markdown
# セキュリティチェックリスト

## 入力検証
- [ ] SQLインジェクション対策
- [ ] XSS対策
- [ ] パストラバーサル対策

## 認証・認可
- [ ] 認証バイパスの可能性
- [ ] 権限チェック漏れ
- [ ] セッション管理

## 機密情報
- [ ] ハードコードされた認証情報
- [ ] ログへの機密情報出力
- [ ] エラーメッセージでの情報漏洩
```

**PERFORMANCE.md:**
```markdown
# パフォーマンスチェックリスト

## データベース
- [ ] N+1クエリ
- [ ] インデックス使用
- [ ] 不要なJOIN

## メモリ
- [ ] メモリリーク
- [ ] 大量データの一括読み込み

## ネットワーク
- [ ] 不要なAPI呼び出し
- [ ] キャッシュ活用
```

**scripts/run-linters.sh:**
```bash
#!/bin/bash
npm run lint 2>&1 || true
npm run type-check 2>&1 || true
```
